"use client";

// Disabled: moving the task tabs between containers caused layout deformation
// and incorrect page-entry rendering on mobile. Keep the original page structure.
export default function TaskTabsScrollFix() {
  return null;
}
